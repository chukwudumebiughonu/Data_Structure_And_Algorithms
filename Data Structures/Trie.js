// A trie is a specialized tree used in searching 
// most often with text, A trie can out perform most 
// of the data structures such as the binarysearchtrees
// hash tables, depending on the type of search
// being perform
// It allows you to know if a word or a part of a word
// exist in a body of text
// the starting point of a trie is an empty root node
// A trie has multiple children unlike binary tree
// example: it is used for searching words on a dictionary
// or for autocomplete
// the big O notation is O (of the length of the word)